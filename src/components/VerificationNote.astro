---
---

<aside class="verification-note">
  <span class="caps note-label">A note on verification</span>
  <p>
    Gigs on this site come from public reporting, parliamentary records, and
    AI-assisted searches. <strong>Verified</strong> entries have been reviewed
    against their sources by a human. <em>Unverified</em> entries (shown
    faded, tagged <em>unverified</em>) have not yet been checked and may
    be inaccurate. Spotted something wrong or missing?
    <a href="/contribute">Help us verify.</a>
  </p>
</aside>

<style>
  .verification-note {
    font-family: var(--font-serif-stack);
    color: var(--color-ink-2);
    background: var(--color-paper-alt);
    padding: var(--space-md) var(--space-lg);
    margin: var(--space-lg) 0 var(--space-xl);
    border: 1px solid var(--color-rule);
    border-radius: var(--radius-sm);
    max-width: var(--measure-wide);
    display: grid;
    gap: var(--space-xs);
  }

  .note-label {
    color: var(--color-ink-2);
  }

  .verification-note p {
    margin: 0;
    font-size: var(--text-small);
    line-height: 1.55;
    max-width: 60ch;
  }

  .verification-note strong {
    color: var(--color-ink);
  }
</style>
