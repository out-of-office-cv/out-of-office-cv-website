logs/
.cron.lock
node_modules
dist/
.astro/
.remember/
.worktrees/

mise.local.toml

# Cron search-throttle state: machine bookkeeping, authoritative in the cron
# worktree, mirrored to the cron-state branch rather than merged via PR.
data/find-state.json
data/verify-state.json
data/*.bak

# Allow .claude/skills/ to be tracked (overrides global ~/.gitignore)
!.claude/
.claude/*
!.claude/skills/
